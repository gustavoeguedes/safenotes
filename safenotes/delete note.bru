meta {
  name: delete note
  type: http
  seq: 7
}

put {
  url: http://localhost:8080/notes/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "meu novo titulo"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
